name: Build

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Install samtools
        run: sudo apt-get update && sudo apt-get install -y samtools

      - name: Run go vet
        run: GOCACHE=/tmp/go-build-cache go vet ./...

      - name: Run tests
        run: GOCACHE=/tmp/go-build-cache go test ./...

      - name: Set version
        id: version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            echo "version=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"
          else
            echo "version=$(git describe --tags --always --dirty)" >> "$GITHUB_OUTPUT"
          fi
          echo "git_hash=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Build binaries
        env:
          LDFLAGS: >-
            -X 'github.com/compgenlab/cgio/internal/cmd.Version=${{ steps.version.outputs.version }}'
            -X 'github.com/compgenlab/cgio/internal/cmd.GitHash=${{ steps.version.outputs.git_hash }}'
        run: |
          mkdir -p bin
          GOOS=darwin  GOARCH=arm64                go build -buildvcs=false -ldflags "$LDFLAGS" -o bin/cgio.darwin_arm64  .
          GOOS=darwin  GOARCH=amd64                go build -buildvcs=false -ldflags "$LDFLAGS" -o bin/cgio.darwin_amd64  .
          GOOS=linux   GOARCH=arm64 CGO_ENABLED=0  go build -buildvcs=false -ldflags "$LDFLAGS" -o bin/cgio.linux_arm64   .
          GOOS=linux   GOARCH=amd64 CGO_ENABLED=0  go build -buildvcs=false -ldflags "$LDFLAGS" -o bin/cgio.linux_amd64   .
          GOOS=windows GOARCH=amd64 CGO_ENABLED=0  go build -buildvcs=false -ldflags "$LDFLAGS" -o bin/cgio.windows_amd64.exe .

      - name: Upload cgio.darwin_arm64
        uses: actions/upload-artifact@v7
        with:
          name: cgio.darwin_arm64
          path: bin/cgio.darwin_arm64

      - name: Upload cgio.darwin_amd64
        uses: actions/upload-artifact@v7
        with:
          name: cgio.darwin_amd64
          path: bin/cgio.darwin_amd64

      - name: Upload cgio.linux_arm64
        uses: actions/upload-artifact@v7
        with:
          name: cgio.linux_arm64
          path: bin/cgio.linux_arm64

      - name: Upload cgio.linux_amd64
        uses: actions/upload-artifact@v7
        with:
          name: cgio.linux_amd64
          path: bin/cgio.linux_amd64

      - name: Upload cgio.windows_amd64
        uses: actions/upload-artifact@v7
        with:
          name: cgio.windows_amd64.exe
          path: bin/cgio.windows_amd64.exe

      - name: Create source archive
        run: |
          VERSION=${{ steps.version.outputs.version }}
          mkdir -p dist
          git archive --format=zip --prefix=cgio-${VERSION}/ HEAD > dist/cgio-${VERSION}-src.zip

      - name: Create tagged release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3
        with:
          files: |
            bin/cgio.darwin_arm64
            bin/cgio.darwin_amd64
            bin/cgio.linux_arm64
            bin/cgio.linux_amd64
            bin/cgio.windows_amd64.exe
            dist/cgio-*-src.zip

      - name: Create dev release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: dev
          name: Development Build
          prerelease: true
          make_latest: false
          body: |
            Automated build from main branch.

            **Commit:** ${{ github.sha }}
            **Version:** ${{ steps.version.outputs.version }}
          files: |
            bin/cgio.darwin_arm64
            bin/cgio.darwin_amd64
            bin/cgio.linux_arm64
            bin/cgio.linux_amd64
            bin/cgio.windows_amd64.exe
            dist/cgio-*-src.zip
